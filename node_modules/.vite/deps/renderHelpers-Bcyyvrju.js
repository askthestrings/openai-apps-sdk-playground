import { r as __toESM } from "./chunk-BoAXSpZd.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region node_modules/@openai/apps-sdk-ui/dist/es/lib/renderHelpers.js
var import_jsx_runtime = require_jsx_runtime();
var import_react = /* @__PURE__ */ __toESM(require_react());
var flattenTextNodes = (children) => {
	const nodes = import_react.Children.toArray(children);
	const result = [];
	let buffer = "";
	const flush = () => {
		if (buffer !== "") {
			result.push(buffer);
			buffer = "";
		}
	};
	for (const node of nodes) {
		if (node == null || typeof node === "boolean") continue;
		if (typeof node === "string" || typeof node === "number") {
			buffer += String(node);
			continue;
		}
		flush();
		result.push(node);
	}
	flush();
	return result;
};
var wrapTextNodeSiblings = (children) => {
	const flattenedChildren = flattenTextNodes(children);
	const childrenCount = import_react.Children.count(flattenedChildren);
	return import_react.Children.map(flattenedChildren, (child) => {
		if (typeof child === "string" && !!child.trim()) {
			if (childrenCount <= 1) return child;
			return (0, import_jsx_runtime.jsx)("span", { children: child });
		}
		if ((0, import_react.isValidElement)(child)) {
			const element = child;
			const { children: innerChildren, ...restProps } = element.props;
			if (innerChildren != null) return (0, import_react.cloneElement)(element, restProps, wrapTextNodeSiblings(innerChildren));
			return element;
		}
		return child;
	});
};
//#endregion
export { wrapTextNodeSiblings as t };

//# sourceMappingURL=renderHelpers-Bcyyvrju.js.map