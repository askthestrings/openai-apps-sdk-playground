import { r as __toESM } from "./chunk-BoAXSpZd.js";
import { t as require_react } from "./react.js";
//#region node_modules/@openai/apps-sdk-ui/dist/es/lib/theme.js
var import_react = /* @__PURE__ */ __toESM(require_react());
function useDocumentTheme() {
	const [theme, setTheme] = (0, import_react.useState)(getDocumentTheme);
	(0, import_react.useEffect)(() => {
		const observer = new MutationObserver(() => {
			setTheme(getDocumentTheme());
		});
		observer.observe(document.documentElement, {
			attributes: true,
			attributeFilter: ["data-theme", "class"],
			characterData: false,
			childList: false,
			subtree: false
		});
		return () => observer.disconnect();
	});
	return theme;
}
function getDocumentTheme() {
	const theme = document.documentElement.getAttribute("data-theme");
	if (theme === "dark" || theme === "light") return theme;
	return document.documentElement.classList.contains("dark") ? "dark" : "light";
}
function applyDocumentTheme(theme) {
	const htmlTag = document.documentElement;
	htmlTag.setAttribute("data-theme", theme);
	htmlTag.style.colorScheme = theme;
}
//#endregion
export { applyDocumentTheme, getDocumentTheme, useDocumentTheme };

//# sourceMappingURL=@openai_apps-sdk-ui_theme.js.map